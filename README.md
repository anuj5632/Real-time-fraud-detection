# Fraud Detection

This repository separates services by branch:

- main: Spring Boot backend in fraud-detection/
- feature/ml-service: ML microservice in ml-service/
- feature/rag-service: RAG microservice in rag-service/

Each branch contains only the files needed for that service.
